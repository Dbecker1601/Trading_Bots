# Trading_Bots

## Databento API Key Hinterlegen

1. Kopiere die Vorlage:
   - `cp .env.example .env`
2. Trage deinen Key in `.env` ein:
   - `DATABENTO_API_KEY=your_databento_api_key_here`

`.env` ist in `.gitignore` enthalten und wird nicht versioniert.
